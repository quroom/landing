## 1. Layout Update

- [x] 1.1 `landing/templates/landing/base.html`의 언어 전환 UI 배치를 CTA와 충돌하지 않게 조정한다.
- [x] 1.2 `landing/static/landing/css/site.css`에 반응형 배치 스타일을 추가/수정한다.

## 2. Regression Tests

- [x] 2.1 `landing/tests/test_landing_pages.py`에 언어 전환 UI/CTA 회귀 방지 검증을 추가한다.

## 3. Verify

- [x] 3.1 `./scripts/verify.sh`를 실행해 전체 검증을 통과한다.
