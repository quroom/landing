#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(git rev-parse --show-toplevel)"

echo "[pre-push] Running deployment safety checks..."
"${ROOT_DIR}/scripts/verify.sh"
echo "[pre-push] Checks passed. Continuing push."
